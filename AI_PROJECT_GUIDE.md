# 校园二手平台 - AI项目开发指南

## 📋 文档目的

本文档旨在为AI助手提供项目级的开发指导，确保不同AI在开发过程中保持一致性，避免重复工作和对接失误。

## 🏗️ 项目架构概览

### 技术栈
- **后端**: Django 4.2.28
- **前端**: Bootstrap 5 + HTML5 + JavaScript
- **数据库**: SQLite (开发环境)
- **模板引擎**: Django Templates

### 核心应用模块
```
项目根目录: mysite/
├── config/          # Django项目配置
├── core/            # 核心功能模块
├── items/           # 物品发布与浏览系统
├── users/           # 用户认证与个人中心
├── templates/       # 模板文件
├── static/          # 静态资源
└── media/           # 用户上传文件
```

## 📁 目录结构规范

### 1. 应用目录结构 (必须遵守)
```
应用名称/
├── migrations/          # 数据库迁移文件
├── management/          # 自定义管理命令
├── templates/应用名称/   # 应用专属模板
├── __init__.py
├── admin.py            # 后台管理配置
├── apps.py             # 应用配置
├── forms.py            # 表单定义
├── models.py           # 数据模型
├── tests.py            # 测试文件
├── urls.py             # URL路由
├── views.py            # 视图函数
└── AI_EXTENSION_GUIDE.md # 应用专属AI指南
```

### 2. 模板目录结构 (必须遵守)
```
templates/
├── base.html           # 基础模板
├── core/               # 核心模块模板
│   └── mainpage.html   # 首页模板
├── items/              # 物品模块模板
│   ├── item_form.html  # 发布/编辑页面
│   ├── item_list.html  # 物品列表
│   ├── item_detail.html # 物品详情
│   └── my_items.html   # 我的物品
└── users/              # 用户模块模板
    ├── login.html      # 登录页面
    ├── register.html   # 注册页面
    └── dashboard.html  # 个人中心
```

### 3. 重要文件命名约定

#### 模板文件命名
- **首页**: `mainpage.html` (不是home.html或homepage.html)
- **列表页**: `item_list.html` (不是list.html或items.html)
- **详情页**: `item_detail.html` (不是detail.html)
- **表单页**: `item_form.html` (不是create.html或edit.html)

#### URL模式命名
- **首页**: `home` (不是index或main)
- **物品列表**: `item_list` (不是items或list)
- **物品详情**: `item_detail` (不是detail)
- **发布物品**: `item_create` (不是create)

## 💻 代码规范与偏好

### 1. Python代码规范

#### 导入顺序
```python
# 1. Python标准库
import os
import sys

# 2. Django核心模块
from django.shortcuts import render, redirect
from django.contrib.auth.decorators import login_required

# 3. 第三方库
# (本项目暂无第三方库依赖)

# 4. 本地应用模块
from .models import Item, Category
from .forms import ItemForm
```

#### 视图函数装饰器顺序
```python
@login_required  # 权限装饰器在最外层
def item_create(request):
    """发布物品视图"""
    # 函数实现
```

#### 模型字段定义顺序
```python
class Item(models.Model):
    # 1. 选择项定义
    STATUS_CHOICES = [
        ('active', '在售'),
        ('sold', '已售'),
    ]
    
    # 2. 核心字段
    title = models.CharField(max_length=200)
    description = models.TextField()
    
    # 3. 关联字段
    seller = models.ForeignKey(User, on_delete=models.CASCADE)
    category = models.ForeignKey(Category, on_delete=models.CASCADE)
    
    # 4. 状态字段
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='active')
    
    # 5. 时间字段
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

### 2. HTML模板规范

#### 模板继承结构
```html
{% extends 'base.html' %}
{% load static %}

{% block title %}页面标题 - 校园二手{% endblock %}

{% block content %}
<!-- 页面内容 -->
{% endblock %}
```

#### Bootstrap类使用规范
- **容器**: 使用 `container` 类
- **间距**: 使用 `mt-4`, `mb-3`, `p-3` 等间距类
- **按钮**: 使用 `btn btn-primary`, `btn btn-secondary`
- **卡片**: 使用 `card`, `card-body`, `card-header`

#### JavaScript事件处理
```javascript
// 使用事件委托，避免重复绑定
document.addEventListener('click', function(e) {
    if (e.target.matches('.category-card')) {
        const categoryId = e.target.getAttribute('data-category-id');
        window.location.href = `/list/?category=${categoryId}`;
    }
});
```

### 3. URL路由规范

#### URL模式定义
```python
# items/urls.py
urlpatterns = [
    path('', views.home, name='home'),                    # 首页
    path('create/', views.item_create, name='item_create'), # 发布物品
    path('<int:pk>/', views.item_detail, name='item_detail'), # 物品详情
    path('list/', views.item_list, name='item_list'),      # 物品列表
]

# config/urls.py - 应用包含
urlpatterns = [
    path('', include('core.urls')),      # 核心模块
    path('items/', include('items.urls')), # 物品模块
    path('users/', include('users.urls')), # 用户模块
]
```

## 🔄 开发流程规范

### 1. 新功能开发流程

#### 步骤1: 检查现有结构
```bash
# 查看应用结构
ls items/

# 查看模板结构
ls templates/items/

# 查看URL配置
cat items/urls.py
```

#### 步骤2: 遵循命名约定
- 新模板文件放在 `templates/应用名称/` 目录下
- 使用统一的文件命名规范
- 遵循现有的URL模式命名

#### 步骤3: 保持一致性
- 使用相同的Bootstrap类
- 遵循相同的代码结构
- 保持相同的用户交互模式

### 2. 修改现有功能流程

#### 步骤1: 理解现有逻辑
```python
# 查看视图函数
cat items/views.py

# 查看模板文件
cat templates/items/item_form.html
```

#### 步骤2: 检查关联文件
- 修改视图时检查URL配置
- 修改模板时检查静态资源
- 修改模型时检查表单和视图

#### 步骤3: 测试修改影响
- 启动开发服务器测试
- 检查相关功能是否正常
- 验证用户流程完整性

## 🚫 禁止行为与常见错误

### 1. 文件命名错误
- ❌ `homepage.html` (错误)
- ✅ `mainpage.html` (正确)

- ❌ `list.html` (错误)  
- ✅ `item_list.html` (正确)

- ❌ `create.html` (错误)
- ✅ `item_form.html` (正确)

### 2. URL模式错误
- ❌ `path('', views.index, name='index')` (错误)
- ✅ `path('', views.home, name='home')` (正确)

- ❌ `path('items/', views.items, name='items')` (错误)
- ✅ `path('list/', views.item_list, name='item_list')` (正确)

### 3. 模板位置错误
- ❌ 在根目录创建模板文件
- ✅ 在对应应用模板目录创建文件

## 🔍 快速对接检查清单

### 开始开发前检查
- [ ] 确认应用目录结构
- [ ] 检查模板文件位置
- [ ] 验证URL模式命名
- [ ] 查看现有代码风格

### 开发完成后检查
- [ ] 测试所有相关功能
- [ ] 验证模板继承正确
- [ ] 检查静态资源加载
- [ ] 确认用户流程完整

## 📚 各模块专属指南

### 核心模块 (core/)
- **指南文件**: `core/AI_EXTENSION_GUIDE.md`
- **主要功能**: 首页展示、基础配置
- **模板文件**: `templates/core/mainpage.html`

### 物品模块 (items/)
- **指南文件**: `items/AI_DEVELOPMENT_GUIDE.md`
- **主要功能**: 物品发布、浏览、管理
- **特色功能**: 图片裁剪系统

### 用户模块 (users/)
- **指南文件**: `users/AI_EXTENSION_GUIDE.md`
- **主要功能**: 用户认证、个人中心
- **特色功能**: 头像和背景自定义

## 💡 AI开发提示

### 优先检查事项
1. **模板位置**: 确保新模板放在正确目录
2. **URL命名**: 使用统一的URL模式名称
3. **代码风格**: 遵循现有的代码结构
4. **功能测试**: 修改后务必进行完整测试

### 避免重复工作
- 开发前先查看现有功能
- 修改前理解现有逻辑
- 保持与现有系统的一致性

### 问题解决流程
1. 查看相关AI指南文档
2. 检查代码规范和命名约定
3. 测试功能完整性
4. 更新相关文档

---

## 📞 紧急联系信息

如遇无法解决的问题，请检查：
1. 项目根目录的本文档
2. 各应用目录的专属AI指南
3. 现有代码的结构和模式

**重要提醒**: 任何修改都必须保持与现有系统的一致性，避免创建重复或冲突的功能模块。